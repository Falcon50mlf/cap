import { SlideShell } from "@/components/pitch/SlideShell";

export const metadata = {
  title: "Cap' — Pitch",
  description: "Découvre le pitch Cap' en 10 slides.",
};

export default function PitchPage() {
  return <SlideShell />;
}
